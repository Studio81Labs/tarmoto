name: tarmoto-dev

services:
  postgres:
    container_name: tarmoto-db
    image: postgis/postgis:17-3.4-alpine
    restart: unless-stopped
    # Default host port is 5433 (not 5432) to avoid clashing with any
    # system-local PostgreSQL on the same Docker host. Override with
    # TARMOTO_DATABASE_PORT if 5433 is also taken.
    ports:
      - "${TARMOTO_DATABASE_PORT:-5433}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tarmoto}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tarmoto}
      POSTGRES_DB: ${POSTGRES_DB:-tarmoto}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tarmoto}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    container_name: tarmoto-redis
    image: redis:8-alpine
    restart: unless-stopped
    # Default host port is 6380 (not 6379) to avoid clashing with any
    # system-local Redis or another dev container on 6379.
    ports:
      - "${TARMOTO_REDIS_PORT:-6380}:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # S3-compatible storage for testing the `s3` storage driver
  # locally. Off by default so the standard `pnpm db:up` flow stays
  # cheap; opt in with `pnpm db:up --profile s3` (or
  # `docker compose --profile s3 up -d`) when iterating on avatar /
  # review-photo / data-export storage. Once running, point the
  # backend at it with:
  #
  #   TARMOTO_STORAGE_DRIVER=s3
  #   TARMOTO_S3_BUCKET=tarmoto
  #   TARMOTO_S3_REGION=us-east-1
  #   TARMOTO_S3_ENDPOINT=http://localhost:9000
  #   TARMOTO_S3_ACCESS_KEY_ID=minioadmin
  #   TARMOTO_S3_SECRET_ACCESS_KEY=minioadmin
  #   TARMOTO_S3_FORCE_PATH_STYLE=true
  #
  # The console (default port 9001) is the easy way to create the
  # `tarmoto` bucket on first run. See `docs/process/runbook.md` for
  # the production setup.
  minio:
    container_name: tarmoto-minio
    image: minio/minio:latest
    profiles: ["s3"]
    restart: unless-stopped
    ports:
      - "${TARMOTO_S3_PORT:-9000}:9000"
      - "${TARMOTO_S3_CONSOLE_PORT:-9001}:9001"
    environment:
      MINIO_ROOT_USER: ${TARMOTO_S3_ACCESS_KEY_ID:-minioadmin}
      MINIO_ROOT_PASSWORD: ${TARMOTO_S3_SECRET_ACCESS_KEY:-minioadmin}
    command: server /data --console-address ":9001"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 5

  valhalla:
    container_name: tarmoto-valhalla
    # Opt-in profile: a plain `docker compose up -d` (e.g. `pnpm db:up`) skips
    # this service so a DB boot never triggers the heavy first-run PBF download +
    # tile build. Start it explicitly with `--profile routing up valhalla` (or
    # by naming the service: `up valhalla`).
    profiles: ["routing"]
    # nilsnolde/docker-valhalla is the maintained continuation of the
    # archived gis-ops registry (same project, arm64-native builds).
    image: ghcr.io/nilsnolde/docker-valhalla/valhalla:latest
    ports:
      - "8002:8002"
    volumes:
      - ../valhalla/custom_files:/custom_files
    environment:
      # Tiles build from the PRE-MERGED corridor extract in
      # infra/valhalla/custom_files/corridor.osm.pbf (CZ+AT+SI+HR+HU,
      # ~2.4 GB). Feeding valhalla the five raw Geofabrik extracts crashes
      # its 3.5.x tile build on border-overlap tiles (each extract ships
      # buffered, duplicated border data; symptom: vector::_M_range_check,
      # valhalla/valhalla#4904) — osmium-merging them first dedupes the
      # overlaps. Regenerate with:
      #   docker run --rm -v "$PWD/infra/valhalla/custom_files:/data" \
      #     stefda/osmium-tool osmium merge /data/<extract>.osm.pbf ... \
      #     -o /data/corridor.osm.pbf --overwrite
      # then delete valhalla_tiles/ + file_hashes.txt and restart. Croatia
      # was last taken from the 2026-01-01 snapshot. No tile_urls: the
      # image builds from the local file.
      # Must be False so the FIRST run actually builds tiles from the pbf.
      # (True = "ignore the pbf, load existing tiles" — which crash-loops on an
      # empty volume because there are no tiles yet.)
      use_tiles_ignore_pbf: "False"
      build_elevation: "False"
      # Admins OFF: the graph-enhance stage crashes deterministically on two
      # Croatian tiles (Neum-corridor / Istria admin polygons) with
      # vector::_M_range_check when admin lookup is enabled — reproduced on
      # both the archived gis-ops and current nilsnolde images. Costs
      # country-aware access defaults + border penalties, which local dev
      # doesn't need. Re-try True after a valhalla release fixes the
      # admin-polygon handling for this data.
      build_admins: "False"
      build_time_zones: "False"
      server_threads: "2"
    restart: unless-stopped

  graphhopper:
    container_name: tarmoto-graphhopper
    # Same opt-in `routing` profile as Valhalla: a plain `docker compose up -d`
    # (`pnpm db:up`) skips it so a DB boot never triggers the heavy first-run
    # graph import. Start explicitly with `--profile routing up graphhopper`.
    #
    # The strategic engine (ADR-0004): request-time `custom_model` avoidances
    # today, our own road-quality weighting later. Unlike the Valhalla image,
    # GraphHopper does NOT auto-download the OSM extract — drop
    # `czech-republic-latest.osm.pbf` into infra/graphhopper first (see its
    # README). Pin the tag to keep the config schema in sync with the image.
    profiles: ["routing"]
    # Pinned (not `latest`, which is a nightly from GraphHopper master whose
    # config / custom-model schema can change under us). 10.2 is multi-arch
    # (linux/amd64 + linux/arm64) so it runs natively on Apple-Silicon dev
    # machines — 10.0 is amd64-only. Bump deliberately + retest config.yml.
    image: israelhikingmap/graphhopper:10.2
    ports:
      - "8989:8989"
    volumes:
      - ../graphhopper:/data
    environment:
      # The entrypoint defaults Java to -Xmx1g, which OOMs importing a country
      # extract (the ~900 MB Czech PBF + CH/LM prep). Give it room; tune down
      # for a smaller subregion or up for a larger extract.
      JAVA_OPTS: "-Xms1g -Xmx4g"
    # First run imports the PBF into /data/graph-cache (slow, one-time); later
    # runs reuse it. `-o` is REQUIRED: the image entrypoint defaults the graph
    # cache to /data/default-gh unless given, which would ignore our path and
    # break the documented `rm graph-cache` re-import. `-i` supplies the input.
    command:
      - "-i"
      - "/data/czech-republic-latest.osm.pbf"
      - "-c"
      - "/data/config.yml"
      - "-o"
      - "/data/graph-cache"
      - "--host"
      - "0.0.0.0"
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
  miniodata:
