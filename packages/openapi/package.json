{
  "name": "@tarmoto/openapi",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI spec for the Tarmoto backend (yaml + Postman collection). TypeScript types and clients live in @tarmoto/openapi-client.",
  "scripts": {
    "generate": "bash scripts/generate.sh",
    "postman": "node scripts/generate-postman.js",
    "test": "vitest run",
    "validate": "npx @redocly/cli lint --config .redocly.yaml openapi.yaml"
  },
  "devDependencies": {
    "js-yaml": "^4.3.1",
    "vitest": "^4.1.10"
  }
}
