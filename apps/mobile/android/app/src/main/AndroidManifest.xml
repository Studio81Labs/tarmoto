<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
      US-280 — runtime permissions Tarmoto actually requests:
        * Foreground location for the live ride HUD and the map tab.
        * Coarse location for the network-provider fallback when GPS hasn't
          acquired a fix yet (cold start in a covered car park).
        * Background location so a ride keeps recording while the screen is
          locked or the rider switches apps mid-ride.
        * BODY_SENSORS for high-rate motion (lean angle, road-quality
          classifier) — the default `<uses-permission>` for accelerometer
          access is implicit, but the high-rate stream we ingest is gated.
        * POST_NOTIFICATIONS (Android 13+) for hazard alerts, ride
          reminders, and the foreground-service notification.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Foreground service for ride recording. Android 14 (API 34) split
      FOREGROUND_SERVICE into typed companions; rides require the location
      type so the OS keeps `watchPosition` alive while backgrounded.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!--
      WAKE_LOCK is declared by @thehale/react-native-keep-awake's manifest and merged
      in, but re-declaring keeps it visible to any audit that diffs the app
      manifest in isolation (e.g. the Play Console pre-launch report).
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      GPS feature flag. Filters non-GPS hardware out of the Play Store
      eligibility list (no point shipping Tarmoto to a device that can't
      acquire a fix). Cellular-only tablets keep getting filtered, which
      matches the rider-on-bike target.
    -->
    <uses-feature
      android:name="android.hardware.location.gps"
      android:required="true" />

    <!--
      US-17 — Android Auto.
      Phones (not head units) are the primary target; Android Auto runs as
      a projection, so the automotive feature is optional. Without this
      flag, Play Console rejects the build for non-automotive devices.
    -->
    <uses-feature
      android:name="android.hardware.type.automotive"
      android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!--
        Tells the AA host this app provides templated screens (rather than
        media or messaging). Points to res/xml/automotive_app_desc.xml.
      -->
      <meta-data
        android:name="com.google.android.gms.car.application"
        android:resource="@xml/automotive_app_desc" />

      <!--
        Pin the AA Car App API level Tarmoto compiled against. The
        react-native-carplay package's manifest contributes a default of
        1; we ship templates (Map / Pane / Search / Navigation) introduced
        through API level 5, so we lift the floor here. AA hosts that
        report a lower API level will silently skip the connection rather
        than crash on an unrecognised template.
      -->
      <meta-data
        android:name="androidx.car.app.minCarApiLevel"
        android:value="5"
        tools:replace="android:value" />

      <!--
        CarAppService and the automotive intent filter are also declared
        in the react-native-carplay package's manifest and merged into
        this app at build time (see node_modules/react-native-carplay/
        android/src/main/AndroidManifest.xml). Keeping the merged service
        the single source of truth avoids two registrations of the same
        component name. If the package is removed, declare the service
        explicitly here pointing at .CarPlayService.
      -->

      <!-- The launcher activity must be exported for the home screen to start
           it; its only intent-filter is MAIN/LAUNCHER, so nothing beyond the
           launcher intent reaches it.
           nosemgrep: java.android.security.exported_activity.exported_activity -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="tarmoto" />
        </intent-filter>

        <!--
          US-17 follow-up (#343) — Google Assistant App Actions.
          Points the system at `res/xml/shortcuts.xml`, which declares
          the `custom.actions.intent.REPORT_HAZARD` capability and the
          inline inventory of hazard-type synonyms. The Assistant uses
          this metadata to match voice queries like "Hey Google, ask
          Tarmoto to report a pothole" on both Android Auto and the
          phone Assistant; the matched capability fires a
          `tarmoto://hazard/report?preselectedType=<type>` deep link
          aimed at this activity. React Navigation's linking config
          (in src/navigation/RootNavigator.tsx) then routes the URL to
          the HazardReport modal with the type pre-selected.
        -->
        <meta-data
          android:name="android.app.shortcuts"
          android:resource="@xml/shortcuts" />
      </activity>
    </application>
</manifest>
