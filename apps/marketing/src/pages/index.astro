---
import Base from "../layouts/Base.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import PlannerMap from "../components/PlannerMap.astro";
import WaitlistForm from "../components/WaitlistForm.tsx";

const apiUrl = import.meta.env.PUBLIC_API_URL ?? "";

const pillars = [
  {
    n: "01",
    t: "Plan on a real screen",
    s: "Planning a 200 km ride on a phone is painful. Tarmoto is desktop-first: big map, full keyboard, drag points, branch alternatives, compare drafts side by side. The way you actually plan a Saturday.",
  },
  {
    n: "02",
    t: "Twisty, not broken",
    s: "A great road is twisty and smooth. Tarmoto biases your route toward better asphalt, away from rough or unknown surfaces, and lets you set the floor. No more 30 km of cratered tarmac mid-loop.",
  },
  {
    n: "03",
    t: "Send it. Ride it.",
    s: "When the route is right, send it to your phone. Offline-ready, simple turn cues. The route is the artifact — the phone companion keeps the ride simple.",
  },
];

const qualityScale = [
  { q: 1, label: "Avoid", desc: "Broken surface or flagged hazard." },
  { q: 2, label: "Rough", desc: "Patchy. Slow-speed only." },
  { q: 3, label: "OK", desc: "Commutable. Fine in the dry." },
  { q: 4, label: "Great", desc: "Smooth, well-swept asphalt." },
  { q: 5, label: "Hero", desc: "Ribbon tarmac. Worth the detour." },
];
const Q_VARS = ["var(--q1)", "var(--q2)", "var(--q3)", "var(--q4)", "var(--q5)"];

const workflowSteps = [
  ["01", "Plan", "Set start, end, stops. Build a first draft quickly."],
  ["02", "Tune", "Curves, surface, scenery, avoidances. Adjust the route before you ride."],
  ["03", "Sync", "Send the route to your phone and keep it available offline."],
  ["04", "Ride", "Mount the phone. Open the synced route. Ride without signal anxiety."],
] as const;

const workflowExtras = [
  {
    t: "GPX in & out",
    d: "Import existing GPX files and export routes for tools and devices that understand GPX.",
  },
  {
    t: "Multi-stop drafts",
    d: "Plan multi-stop routes, keep alternatives, and compare distance, climb, and surface mix.",
  },
  {
    t: "Offline riding",
    d: "The goal is simple: sync before you leave, then keep map, route, and cues usable without signal.",
  },
];

const features = [
  { t: "Branchable drafts", s: "Fork a route idea, compare alternatives, and keep the better one." },
  { t: "Elevation profile", s: "See climb, descent, and grade before committing to a route." },
  { t: "Road-type aware", s: "Asphalt, paved, unknown, gravel, cobble — visible instead of hidden." },
  { t: "GPX import / export", s: "Bring routes in, tune them, and export them back out when needed." },
  { t: "Distance & time budget", s: "Planned tooling for shaping routes around distance, time, and return constraints." },
  { t: "Companion app", s: "iOS and Android companion for opening synced routes and riding offline." },
];

const milestones = [
  { m: "May 2026", t: "Waitlist opens", state: "current" },
  { m: "Summer 2026", t: "First private beta", state: "future" },
  { m: "Autumn 2026", t: "Wider European beta", state: "future" },
  { m: "Late 2026 / Early 2027", t: "v1.0 target", state: "future" },
];

const tiers = [
  {
    name: "Free",
    price: "€0",
    cadence: "forever",
    pitch: "Plan, save up to 5 routes, sync to one device.",
    feats: [
      "Full planner on web",
      "Up to 5 saved routes",
      "GPX export",
      "Companion app · 1 region offline",
    ],
    highlight: false,
  },
  {
    name: "Pro",
    price: "~€39",
    cadence: "per year · planned",
    pitch: "Unlimited routes, drafts, regions. Supports the work.",
    feats: [
      "Everything in Free",
      "Unlimited routes & drafts",
      "Unlimited offline regions",
      "Compare drafts side-by-side",
      "Priority email support",
    ],
    highlight: true,
  },
];

const faq: Array<[string, string]> = [
  [
    "Where does the road quality data come from?",
    "We start from public OpenStreetMap surface and smoothness tags as a baseline, and estimate quality only where data exists. Beta rider feedback is planned to refine the picture over time. Segments we don't have signal on are clearly marked as \"unknown\" — you can choose to avoid them rather than guess.",
  ],
  [
    "Is this another phone GPS app?",
    "No. Tarmoto starts with planning on a bigger screen — Mac, Windows, or web. The phone companion is for the ride itself: open the route you prepared, keep it available offline, and follow simple cues. We're not trying to make complex route planning happen on a small screen.",
  ],
  [
    "How does it compare to Calimoto, Kurviger, Rever?",
    "They are good apps and we use some of them. Tarmoto is desktop-first, with side-by-side draft comparison, finer control over surface and curvature, and an explicit \"unknown roads\" filter. If you do most of your planning on a phone, those apps may suit you better.",
  ],
  [
    "Can I import my existing GPX routes?",
    "GPX import and export are part of the plan. The goal is to let you bring existing routes into the planner, inspect their surface/quality breakdown where data exists, tune them, and export them again.",
  ],
  [
    "What about privacy?",
    "No ads, no data resale. Routes sync between your devices through your account; rides recorded on the phone stay on the phone unless you choose to share. You'll be able to export or delete your data. We'll publish a full privacy policy before public launch.",
  ],
  [
    "When will it be available?",
    "Waitlist is open now. We're aiming for a first private beta in summer 2026, a wider European beta in autumn, and a v1.0 target in late 2026 or early 2027. These are intentions, not guarantees — we'll write to you when there's an invite to give.",
  ],
];

const trustNote = "BUILT AROUND OPENSTREETMAP DATA";
---

<Base
  title="Tarmoto · A better way to plan motorcycle rides"
  description="Tarmoto is a desktop route planner for motorcyclists who actually plan rides. Twisty without the broken asphalt. Sync it to your phone and go."
>
  <Nav />

  <!-- HERO -->
  <section class="hero">
    <div class="hero-grid">
      <div class="hero-text">
        <div class="hero-badge">
          <span class="hero-badge-dot"></span>
          <span class="mono hero-badge-text">PRIVATE BETA OPENING · SUMMER 2026</span>
        </div>
        <h1 class="serif hero-title">
          Plan motorcycle rides<br />
          on a <em>real screen.</em>
        </h1>
        <p class="hero-sub">
          Tarmoto is a desktop-first route planner for motorcyclists who actually plan rides.
          Built for twisty roads, surface awareness, and fewer surprises. Sync the route to your
          phone companion and ride.
        </p>
        <div id="waitlist" class="hero-waitlist">
          <WaitlistForm client:visible apiUrl={apiUrl} stage="waitlist" />
        </div>
        <div class="hero-platforms">
          <span><span class="hero-platforms-strong">Mac · Windows · Web</span> for planning</span>
          <span class="hero-platforms-sep">·</span>
          <span><span class="hero-platforms-strong">iOS &amp; Android</span> companion for riding</span>
        </div>
      </div>

      <!-- Hero planner panel mock -->
      <div class="hero-panel">
        <div class="hero-panel-frame">
          <div class="hero-panel-chrome">
            <div class="hero-panel-dots">
              <span style="background:#FF5F57"></span>
              <span style="background:#FEBC2E"></span>
              <span style="background:#28C840"></span>
            </div>
            <div class="mono hero-panel-title">tarmoto · Stelvio loop · draft 3</div>
            <div class="hero-panel-actions">
              <span class="mono hero-panel-action">SAVE</span>
              <span class="mono hero-panel-action primary">SEND TO PHONE</span>
            </div>
          </div>

          <div class="hero-panel-body">
            <aside class="hero-panel-sidebar">
              <span class="stamp" style="font-size: 9.5px">Road preferences</span>
              <div class="pref-slider">
                <div class="pref-slider-row">
                  <span class="pref-slider-label">More curves</span>
                  <span class="mono pref-slider-value">78</span>
                </div>
                <div class="pref-slider-track">
                  <span class="pref-slider-fill" style="width: 78%"></span>
                  <span class="pref-slider-thumb" style="left: 78%"></span>
                </div>
              </div>
              <div class="pref-slider">
                <div class="pref-slider-row">
                  <span class="pref-slider-label">Better asphalt</span>
                  <span class="mono pref-slider-value">92</span>
                </div>
                <div class="pref-slider-track">
                  <span class="pref-slider-fill" style="width: 92%"></span>
                  <span class="pref-slider-thumb" style="left: 92%"></span>
                </div>
              </div>
              <div class="pref-slider">
                <div class="pref-slider-row">
                  <span class="pref-slider-label">Scenic</span>
                  <span class="mono pref-slider-value">64</span>
                </div>
                <div class="pref-slider-track">
                  <span class="pref-slider-fill" style="width: 64%"></span>
                  <span class="pref-slider-thumb" style="left: 64%"></span>
                </div>
              </div>
              <div class="hero-panel-divider"></div>
              <span class="stamp" style="font-size: 9.5px">Avoid</span>
              <div class="pref-toggle on">
                <span>Unknown roads</span>
                <span class="pref-toggle-switch"><span></span></span>
              </div>
              <div class="pref-toggle on">
                <span>Villages</span>
                <span class="pref-toggle-switch"><span></span></span>
              </div>
              <div class="pref-toggle on">
                <span>Motorways</span>
                <span class="pref-toggle-switch"><span></span></span>
              </div>
              <div class="pref-toggle off">
                <span>Gravel</span>
                <span class="pref-toggle-switch"><span></span></span>
              </div>
            </aside>

            <div class="hero-panel-map">
              <PlannerMap />
              <div class="map-overlay map-overlay-tl">
                <span class="stamp" style="font-size: 9px">Route</span>
                <div class="map-overlay-strong">4 stops · 187 km · 4h 38m</div>
              </div>
              <div class="map-overlay map-overlay-bottom">
                <div>
                  <span class="stamp" style="font-size: 9px">Asphalt mix</span>
                  <div class="asphalt-mix">
                    <span style="flex: 6; background: var(--q5)"></span>
                    <span style="flex: 3; background: var(--q4)"></span>
                    <span style="flex: 1; background: var(--q3)"></span>
                  </div>
                  <div class="mono asphalt-mix-text">60% great · 30% ok · 10% rough</div>
                </div>
                <div class="map-overlay-divider"></div>
                <div>
                  <span class="stamp" style="font-size: 9px">Climb</span>
                  <div class="map-overlay-strong">+2,140 m</div>
                </div>
                <div class="map-overlay-divider"></div>
                <div>
                  <span class="stamp" style="font-size: 9px">Curves / km</span>
                  <div class="map-overlay-strong">3.8</div>
                </div>
                <div class="map-overlay-zoom">
                  <span>−</span>
                  <span>+</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- TRUST STRIP -->
  <section class="trust-strip">
    <div class="container trust-inner">
      <span class="stamp">Foundation</span>
      <div class="trust-logos">
        <span>{trustNote}</span>
      </div>
      <span class="mono trust-made">EARLY BETA · WAITLIST OPEN</span>
    </div>
  </section>

  <!-- PILLARS -->
  <section class="section">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 01</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">What it is</span>
      </div>
      <h2 class="serif h-display section-h fade-up">Why Tarmoto exists.</h2>
      <div class="pillar-grid fade-up">
        {
          pillars.map((p) => (
            <div class="pillar-card">
              <div class="mono pillar-num">{p.n}</div>
              <div class="pillar-title">{p.t}</div>
              <div class="pillar-body">{p.s}</div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- PLANNER DEEP -->
  <section id="planner" class="planner-deep">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 02</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">The planner</span>
      </div>
      <h2 class="serif h-display section-h fade-up">Tune the route. Don't redraw it.</h2>
      <p class="section-lede fade-up">
        You set the start, the end, and a few stops. Tarmoto draws a first draft. From there you
        adjust the dials — curvature, surface, village density, scenery — and the route re-paths in
        place.
      </p>

      <div class="planner-deep-grid fade-up">
        <div class="planner-deep-map">
          <div class="planner-deep-map-frame">
            <PlannerMap />
            <div class="planner-deep-stops">
              {
                ["Stelvio Pass", "Gavia", "Mortirolo", "Bormio"].map((n, i) => (
                  <span class="planner-deep-stop">
                    <span class="mono planner-deep-stop-letter">
                      {String.fromCharCode(65 + i)}
                    </span>
                    {n}
                  </span>
                ))
              }
            </div>
          </div>
        </div>

        <div class="control-rail">
          <div class="control-card">
            <div class="control-card-row">
              <div class="control-card-title">Curvature</div>
              <div class="mono control-card-value">78</div>
            </div>
            <div class="control-card-hint">More twisty</div>
            <div class="bars">
              {
                [2, 3, 4, 3, 5, 4, 5, 5, 4, 5].map((v) => (
                  <span
                    class={v >= 4 ? "bars-active" : ""}
                    style={`height: ${v * 18}%`}
                  />
                ))
              }
            </div>
          </div>

          <div class="control-card">
            <div class="control-card-row">
              <div class="control-card-title">Asphalt minimum</div>
              <div class="mono control-card-value">Great</div>
            </div>
            <div class="control-card-hint">Avoid anything below 4★</div>
            <div class="asphalt-min">
              {
                [1, 2, 3, 4, 5].map((q) => (
                  <div class={`asphalt-min-cell ${q >= 4 ? "active" : ""}`}>
                    <span class="qbars">
                      {[1, 2, 3, 4, 5].map((n) => (
                        <span
                          style={`width: 3px; height: 6.6px; background: ${
                            n <= q ? Q_VARS[q - 1] : "rgba(232,229,222,0.10)"
                          }`}
                        />
                      ))}
                    </span>
                  </div>
                ))
              }
            </div>
          </div>

          <div class="control-card">
            <div class="control-card-row">
              <div class="control-card-title">Avoid</div>
            </div>
            <div class="control-card-hint">Tap to toggle</div>
            <div class="avoid-pills">
              {
                (
                  [
                    ["Villages", true],
                    ["Motorways", true],
                    ["Unknown roads", true],
                    ["Tunnels", false],
                    ["Gravel", false],
                    ["Ferries", false],
                  ] as Array<[string, boolean]>
                ).map(([l, on]) => (
                  <span class={`avoid-pill ${on ? "on" : "off"}`}>{l}</span>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ROAD QUALITY -->
  <section id="road-quality" class="section">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 03</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">Road quality</span>
      </div>
      <h2 class="serif h-display section-h fade-up">A five-point view of the road under you.</h2>
      <p class="section-lede fade-up">
        We estimate road quality where data exists, from <span class="rq-emph">1 (avoid)</span> to <span
          class="rq-emph">5 (hero asphalt)</span
        >, using OpenStreetMap surface and smoothness tags as a baseline. Rider feedback is planned
        to sharpen confidence over time. Segments without signal are clearly marked — you can
        choose to avoid them.
      </p>

      <div class="quality-grid fade-up">
        {
          qualityScale.map(({ q, label, desc }) => (
            <div class="quality-card">
              <span class="qbars">
                {[1, 2, 3, 4, 5].map((n) => (
                  <span
                    style={`width: 6px; height: 13.2px; background: ${
                      n <= q ? Q_VARS[q - 1] : "rgba(232,229,222,0.10)"
                    }`}
                  />
                ))}
              </span>
              <div class="quality-label" style={`color: ${Q_VARS[q - 1]}`}>{label}</div>
              <div class="quality-desc">{desc}</div>
            </div>
          ))
        }
      </div>

      <div class="how-we-know fade-up">
        <span class="stamp stamp-accent">How we know</span>
        <div class="how-we-know-text">
          We start from public OpenStreetMap tags (<span class="mono how-we-know-mono">surface=*</span
          >, <span class="mono how-we-know-mono">smoothness=*</span>) as a baseline. Beta rider
          observations are planned to refine confidence on segments people actually ride. Where we
          don't have signal, we say so — and you can choose to avoid those segments.
        </div>
      </div>
    </div>
  </section>

  <!-- WORKFLOW -->
  <section id="workflow" class="workflow">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 04</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">Workflow</span>
      </div>
      <h2 class="serif h-display section-h fade-up">
        Four steps. <em>Plan. Tune. Sync. Ride.</em>
      </h2>
      <p class="section-lede fade-up">
        The desktop is where the route gets shaped. The phone is where it gets used. The two stay
        in sync — once the route is synced, the riding flow works offline.
      </p>

      <div class="workflow-steps fade-up">
        {
          workflowSteps.map(([n, t, d]) => (
            <div class="workflow-step">
              <div class="mono workflow-step-num">{n}</div>
              <div class="workflow-step-title">{t}</div>
              <div class="workflow-step-desc">{d}</div>
            </div>
          ))
        }
      </div>

      <div class="workflow-sync fade-up">
        <div class="workflow-desktop">
          <div class="workflow-desktop-chrome">
            <div class="workflow-desktop-dots">
              <span style="background:#FF5F57"></span>
              <span style="background:#FEBC2E"></span>
              <span style="background:#28C840"></span>
            </div>
            <div class="mono workflow-desktop-title">tarmoto.app — Stelvio loop</div>
          </div>
          <div class="workflow-desktop-map">
            <PlannerMap />
            <div class="workflow-synced">
              <span class="workflow-synced-dot"></span>
              <span class="mono workflow-synced-text">SYNCED · 14:22</span>
            </div>
          </div>
        </div>

        <div class="workflow-arrow">
          <span class="stamp stamp-accent" style="font-size: 10px">SYNC</span>
          <svg width="60" height="40" viewBox="0 0 60 40" aria-hidden="true">
            <path
              d="M 6 20 L 50 20 M 42 12 L 50 20 L 42 28"
              stroke="var(--accent)"
              stroke-width="1.5"
              fill="none"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
          <div class="mono workflow-arrow-note">OFFLINE READY</div>
        </div>

        <div class="workflow-phone-wrap">
          <div class="phone">
            <div class="phone-screen">
              <div class="phone-status">
                <span class="mono phone-time">9:41</span>
                <span class="mono phone-icons">● ▲</span>
              </div>
              <div class="phone-header">
                <span class="stamp" style="font-size: 9px">Today</span>
                <div class="phone-route">Stelvio loop</div>
                <div class="phone-meta">187 km · 4h 38m</div>
              </div>
              <div class="phone-map">
                <PlannerMap />
              </div>
              <div class="phone-cta">
                <span class="phone-cta-icon">▶</span>
                <div>
                  <div class="phone-cta-title">Start ride</div>
                  <div class="phone-cta-sub">Offline · simple cues</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="workflow-extras fade-up">
        {
          workflowExtras.map((f) => (
            <div class="workflow-extra">
              <div class="workflow-extra-title">{f.t}</div>
              <div class="workflow-extra-desc">{f.d}</div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="section">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 05</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">Capabilities</span>
      </div>
      <h2 class="serif h-display section-h fade-up">Just the tools planning a real ride needs.</h2>
      <div class="feature-grid fade-up">
        {
          features.map((f) => (
            <div class="feature-cell">
              <div class="feature-cell-title">{f.t}</div>
              <div class="feature-cell-desc">{f.s}</div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- WHY THIS EXISTS -->
  <section class="founder">
    <div class="founder-inner">
      <span class="stamp">Why this exists</span>
      <div class="serif founder-quote">
        “Most motorcycle route apps start on the phone. That is fine for navigation, but painful
        for planning. Tarmoto starts on a real screen: bigger map, better context, more control,
        and fewer surprises once you are already on the road.”
      </div>
      <div class="founder-credit">
        <div class="founder-credit-text">
          <div class="founder-role">
            Built by riders, for riders planning longer weekend routes.
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ROADMAP -->
  <section class="section">
    <div class="container">
      <div class="roadmap-grid fade-up">
        <div>
          <div class="eyebrow">
            <span class="eyebrow-num">§ 06</span>
            <span class="eyebrow-rule"></span>
            <span class="stamp">Where we are</span>
          </div>
          <h2 class="serif roadmap-h">Honest about<br />the state of things.</h2>
          <p class="roadmap-lede">
            We're a small team building Tarmoto in the open. A first private beta is planned for
            summer, a wider European beta in autumn, with v1.0 targeted for late 2026 or early
            2027. These are intentions — we'll say so when something slips.
          </p>
        </div>
        <div class="roadmap-card">
          {
            milestones.map((s) => (
              <div class="roadmap-row">
                <span
                  class={`roadmap-dot ${s.state}`}
                  aria-hidden="true"
                />
                <span class="mono roadmap-month">{s.m.toUpperCase()}</span>
                <span class={`roadmap-label ${s.state}`}>{s.t}</span>
                {s.state === "current" && <span class="mono roadmap-now">NOW</span>}
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <!-- PRICING -->
  <section id="pricing" class="pricing">
    <div class="container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 07</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">Pricing intent</span>
      </div>
      <h2 class="serif h-display section-h fade-up">
        One subscription. <em>That's the whole business.</em>
      </h2>
      <p class="section-lede fade-up">
        Pricing is directional. Beta is free. We will confirm the final plan before charging anyone.
        No ads, no data resale.
      </p>

      <div class="pricing-grid fade-up">
        {
          tiers.map((t) => (
            <div class={`pricing-card ${t.highlight ? "highlight" : ""}`}>
              <div class="pricing-card-head">
                <div class="pricing-card-name">{t.name}</div>
                {t.highlight && (
                  <span class="mono pricing-card-rec">PLANNED</span>
                )}
              </div>
              <div class="pricing-card-pitch">{t.pitch}</div>
              <div class="pricing-card-price">
                <div class="pricing-card-amount">{t.price}</div>
                <span class="mono pricing-card-cadence">{t.cadence}</span>
              </div>
              <div class="pricing-card-feats">
                {t.feats.map((f) => (
                  <div class="pricing-card-feat">
                    <span class="pricing-card-bullet">·</span>
                    <span>{f}</span>
                  </div>
                ))}
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="faq">
    <div class="container faq-container">
      <div class="eyebrow fade-up">
        <span class="eyebrow-num">§ 08</span>
        <span class="eyebrow-rule"></span>
        <span class="stamp">FAQ</span>
      </div>
      <h2 class="serif h-display section-h fade-up">Questions, answered plainly.</h2>
      <div class="faq-list fade-up">
        {
          faq.map(([q, a], i) => (
            <details class="faq-item" open={i === 0}>
              <summary>
                <span class="faq-q">{q}</span>
                <span class="faq-toggle" aria-hidden="true">
                  <span class="faq-toggle-plus">+</span>
                  <span class="faq-toggle-minus">−</span>
                </span>
              </summary>
              <div class="faq-a">{a}</div>
            </details>
          ))
        }
      </div>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="final-cta">
    <div class="final-cta-inner">
      <span class="stamp">First private beta · Summer 2026</span>
      <h2 class="serif final-cta-h">
        Plan a ride <em>worth riding.</em>
      </h2>
      <p class="final-cta-lede">
        We're inviting riders in small batches. One email when your turn comes around.
      </p>
      <div class="final-cta-form">
        <WaitlistForm client:visible apiUrl={apiUrl} stage="waitlist" />
      </div>
    </div>
  </section>

  <Footer />
</Base>

<style>
  /* ============= section frame ============= */
  .section {
    padding: 120px 0;
  }

  .section-h {
    font-size: clamp(34px, 5vw, 56px);
    max-width: 920px;
  }

  .section-lede {
    font-size: 18px;
    color: var(--dim);
    max-width: 660px;
    margin-top: 20px;
    line-height: 1.55;
  }

  /* ============= HERO ============= */
  .hero {
    position: relative;
    border-bottom: 1px solid var(--line);
  }

  .hero-grid {
    max-width: 1320px;
    margin: 0 auto;
    padding: 90px 32px 110px;
    display: grid;
    grid-template-columns: 1.05fr 1.1fr;
    gap: 64px;
    align-items: center;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 7px 14px 7px 10px;
    border-radius: 999px;
    border: 1px solid var(--line-2);
    font-size: 12px;
  }

  .hero-badge-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 4px rgba(255, 106, 26, 0.13);
  }

  .hero-badge-text {
    font-size: 10.5px;
    letter-spacing: 0.13em;
    color: var(--dim);
  }

  .hero-title {
    font-size: clamp(48px, 8vw, 86px);
    margin: 24px 0 0;
    letter-spacing: -0.025em;
    line-height: 0.98;
    font-weight: 400;
  }

  .hero-title em {
    font-style: italic;
    color: var(--accent);
  }

  .hero-sub {
    font-size: 19px;
    color: var(--dim);
    max-width: 560px;
    margin-top: 28px;
    line-height: 1.55;
  }

  .hero-waitlist {
    margin-top: 36px;
  }

  .hero-platforms {
    display: flex;
    gap: 28px;
    margin-top: 32px;
    color: var(--mute);
    font-size: 13px;
    flex-wrap: wrap;
  }

  .hero-platforms-strong {
    color: var(--text);
  }

  .hero-platforms-sep {
    color: var(--faint);
  }

  /* Hero panel */
  .hero-panel {
    position: relative;
  }

  .hero-panel-frame {
    background: var(--panel);
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid var(--line-2);
    box-shadow:
      0 40px 100px -30px rgba(0, 0, 0, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .hero-panel-chrome {
    padding: 12px 16px;
    border-bottom: 1px solid var(--line);
    display: flex;
    align-items: center;
    gap: 12px;
    background: var(--panel-2);
  }

  .hero-panel-dots {
    display: flex;
    gap: 6px;
  }

  .hero-panel-dots span {
    width: 11px;
    height: 11px;
    border-radius: 50%;
    opacity: 0.85;
  }

  .hero-panel-title {
    font-size: 11px;
    color: var(--mute);
    letter-spacing: 0.4px;
    margin-left: 8px;
  }

  .hero-panel-actions {
    margin-left: auto;
    display: flex;
    gap: 6px;
  }

  .hero-panel-action {
    font-size: 10px;
    color: var(--mute);
    border: 1px solid var(--line-2);
    padding: 3px 8px;
    border-radius: 4px;
    letter-spacing: 0.06em;
  }

  .hero-panel-action.primary {
    color: var(--bg);
    background: var(--accent);
    font-weight: 600;
    border-color: transparent;
  }

  .hero-panel-body {
    display: grid;
    grid-template-columns: 210px 1fr;
    min-height: 460px;
  }

  .hero-panel-sidebar {
    background: var(--panel-2);
    border-right: 1px solid var(--line);
    padding: 18px 16px;
  }

  .hero-panel-divider {
    height: 1px;
    background: var(--line);
    margin: 14px 0;
  }

  .pref-slider {
    margin-top: 12px;
  }

  .pref-slider-row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
  }

  .pref-slider-label {
    font-size: 12.5px;
    color: var(--text);
  }

  .pref-slider-value {
    font-size: 10.5px;
    color: var(--mute);
  }

  .pref-slider-track {
    margin-top: 6px;
    height: 2px;
    background: rgba(232, 229, 222, 0.10);
    border-radius: 1px;
    position: relative;
  }

  .pref-slider-fill {
    position: absolute;
    inset: 0;
    background: var(--accent);
    border-radius: 1px;
  }

  .pref-slider-thumb {
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--accent);
    border: 2px solid var(--panel-2);
  }

  .pref-toggle {
    margin-top: 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .pref-toggle span:first-child {
    font-size: 12.5px;
  }

  .pref-toggle.on span:first-child {
    color: var(--text);
  }

  .pref-toggle.off span:first-child {
    color: var(--mute);
  }

  .pref-toggle-switch {
    width: 24px;
    height: 14px;
    border-radius: 8px;
    position: relative;
    background: rgba(232, 229, 222, 0.12);
  }

  .pref-toggle.on .pref-toggle-switch {
    background: var(--accent);
  }

  .pref-toggle-switch span {
    position: absolute;
    top: 2px;
    left: 2px;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--text);
    transition: left 0.2s;
  }

  .pref-toggle.on .pref-toggle-switch span {
    left: 12px;
    background: var(--bg);
  }

  .hero-panel-map {
    position: relative;
    min-height: 460px;
  }

  .map-overlay {
    position: absolute;
    background: rgba(11, 13, 16, 0.78);
    border: 1px solid var(--line-2);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .map-overlay-tl {
    top: 14px;
    left: 14px;
    padding: 8px 12px;
    border-radius: 6px;
  }

  .map-overlay-strong {
    font-size: 13px;
    font-weight: 600;
    margin-top: 3px;
  }

  .map-overlay-bottom {
    bottom: 14px;
    left: 14px;
    right: 14px;
    padding: 12px 14px;
    border-radius: 8px;
    display: flex;
    gap: 18px;
    align-items: center;
  }

  .asphalt-mix {
    display: flex;
    gap: 1px;
    margin-top: 6px;
    height: 6px;
    border-radius: 3px;
    overflow: hidden;
    width: 200px;
  }

  .asphalt-mix-text {
    font-size: 10px;
    color: var(--mute);
    margin-top: 5px;
  }

  .map-overlay-divider {
    width: 1px;
    align-self: stretch;
    background: var(--line);
  }

  .map-overlay-zoom {
    margin-left: auto;
    display: flex;
    gap: 6px;
  }

  .map-overlay-zoom span {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    background: var(--panel-3);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    color: var(--dim);
  }

  /* ============= TRUST STRIP ============= */
  .trust-strip {
    border-bottom: 1px solid var(--line);
  }

  .trust-inner {
    padding: 28px 32px;
    display: flex;
    align-items: center;
    gap: 40px;
    flex-wrap: wrap;
  }

  .trust-logos {
    display: flex;
    gap: 36px;
    flex: 1;
    color: rgba(232, 229, 222, 0.50);
    font-size: 12.5px;
    letter-spacing: 0.10em;
    font-weight: 500;
    flex-wrap: wrap;
  }

  .trust-made {
    font-size: 11px;
    color: var(--mute);
    letter-spacing: 0.08em;
  }

  /* ============= PILLARS ============= */
  .pillar-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 64px;
  }

  .pillar-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 32px;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .pillar-num {
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.13em;
  }

  .pillar-title {
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.014em;
  }

  .pillar-body {
    color: var(--dim);
    font-size: 14.5px;
    line-height: 1.6;
  }

  /* ============= PLANNER DEEP ============= */
  .planner-deep {
    border-top: 1px solid var(--line);
    padding: 120px 0;
    background: linear-gradient(180deg, #0B0D10 0%, #0E1115 100%);
  }

  .planner-deep-grid {
    margin-top: 56px;
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: 28px;
  }

  .planner-deep-map {
    background: var(--panel);
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid var(--line-2);
  }

  .planner-deep-map-frame {
    aspect-ratio: 4 / 3;
    position: relative;
  }

  .planner-deep-stops {
    position: absolute;
    top: 16px;
    left: 16px;
    right: 16px;
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .planner-deep-stop {
    padding: 6px 10px;
    border-radius: 6px;
    background: rgba(11, 13, 16, 0.78);
    border: 1px solid var(--line-2);
    font-size: 12px;
    color: var(--text);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .planner-deep-stop-letter {
    font-size: 9.5px;
    color: var(--accent);
    margin-right: 6px;
  }

  .control-rail {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .control-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 22px;
  }

  .control-card-row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
  }

  .control-card-title {
    font-size: 15px;
    font-weight: 600;
  }

  .control-card-value {
    font-size: 12px;
    color: var(--accent);
  }

  .control-card-hint {
    font-size: 12px;
    color: var(--mute);
    margin-top: 4px;
  }

  .bars {
    margin-top: 16px;
    display: flex;
    gap: 4px;
    align-items: flex-end;
    height: 40px;
  }

  .bars span {
    flex: 1;
    border-radius: 2px;
    background: rgba(232, 229, 222, 0.18);
  }

  .bars span.bars-active {
    background: var(--accent);
  }

  .asphalt-min {
    margin-top: 16px;
    display: flex;
    gap: 6px;
  }

  .asphalt-min-cell {
    flex: 1;
    padding: 6px 0;
    border-radius: 5px;
    text-align: center;
    border: 1px solid transparent;
  }

  .asphalt-min-cell.active {
    background: var(--panel-3);
    border-color: var(--line-2);
  }

  .qbars {
    display: inline-flex;
    gap: 2px;
    align-items: flex-end;
  }

  .qbars span {
    border-radius: 1.5px;
  }

  .avoid-pills {
    margin-top: 16px;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .avoid-pill {
    padding: 6px 11px;
    border-radius: 6px;
    font-size: 12px;
    font-weight: 500;
  }

  .avoid-pill.on {
    background: var(--accent);
    color: var(--bg);
    font-weight: 600;
  }

  .avoid-pill.off {
    background: var(--panel-3);
    color: var(--dim);
    border: 1px solid var(--line-2);
  }

  /* ============= ROAD QUALITY ============= */
  .quality-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 12px;
    margin-top: 56px;
  }

  .quality-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 22px;
  }

  .quality-label {
    font-size: 20px;
    font-weight: 600;
    margin-top: 14px;
    letter-spacing: -0.01em;
  }

  .quality-desc {
    font-size: 12.5px;
    color: var(--dim);
    margin-top: 8px;
    line-height: 1.55;
  }

  .rq-emph {
    color: var(--text);
  }

  .how-we-know {
    margin-top: 32px;
    padding: 20px 24px;
    border-radius: 10px;
    border: 1px solid var(--line);
    background: var(--panel-2);
    display: flex;
    gap: 24px;
    align-items: center;
    flex-wrap: wrap;
  }

  .how-we-know-text {
    font-size: 13.5px;
    color: var(--dim);
    line-height: 1.5;
    flex: 1;
    min-width: 320px;
  }

  .how-we-know-mono {
    color: var(--text);
  }

  /* ============= WORKFLOW ============= */
  .workflow {
    border-top: 1px solid var(--line);
    padding: 120px 0;
    background: var(--panel);
  }

  .workflow-steps {
    margin-top: 56px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    border-top: 1px solid var(--line);
    padding-top: 32px;
  }

  .workflow-step-num {
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.13em;
  }

  .workflow-step-title {
    font-size: 22px;
    font-weight: 600;
    margin-top: 8px;
    letter-spacing: -0.014em;
  }

  .workflow-step-desc {
    font-size: 13.5px;
    color: var(--dim);
    margin-top: 8px;
    line-height: 1.55;
  }

  .workflow-sync {
    margin-top: 64px;
    display: grid;
    grid-template-columns: 1.4fr auto 0.6fr;
    gap: 32px;
    align-items: center;
  }

  .workflow-desktop {
    background: var(--panel-2);
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--line-2);
  }

  .workflow-desktop-chrome {
    padding: 10px 14px;
    border-bottom: 1px solid var(--line);
    display: flex;
    align-items: center;
    gap: 10px;
    background: var(--panel-3);
  }

  .workflow-desktop-dots {
    display: flex;
    gap: 5px;
  }

  .workflow-desktop-dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .workflow-desktop-title {
    font-size: 10.5px;
    color: var(--mute);
    margin-left: 8px;
  }

  .workflow-desktop-map {
    aspect-ratio: 16 / 10;
    position: relative;
  }

  .workflow-synced {
    position: absolute;
    bottom: 16px;
    right: 16px;
    padding: 10px 14px;
    border-radius: 8px;
    background: var(--bg);
    border: 1px solid var(--line-2);
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .workflow-synced-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 4px rgba(255, 106, 26, 0.20);
  }

  .workflow-synced-text {
    font-size: 11px;
    color: var(--text);
    letter-spacing: 0.05em;
  }

  .workflow-arrow {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
  }

  .workflow-arrow-note {
    font-size: 10px;
    color: var(--mute);
    letter-spacing: 0.08em;
  }

  .workflow-phone-wrap {
    display: flex;
    justify-content: center;
  }

  .phone {
    width: 220px;
    height: 440px;
    border-radius: 32px;
    padding: 8px;
    background: #000;
    border: 1px solid var(--line-2);
    box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.6);
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    border-radius: 24px;
    background: var(--bg);
    overflow: hidden;
    position: relative;
    border: 1px solid var(--line);
  }

  .phone-status {
    padding: 12px 14px;
    display: flex;
    justify-content: space-between;
    font-size: 11px;
  }

  .phone-time {
    color: var(--text);
  }

  .phone-icons {
    color: var(--mute);
  }

  .phone-header {
    padding: 6px 14px;
  }

  .phone-route {
    font-size: 16px;
    font-weight: 600;
    margin-top: 2px;
  }

  .phone-meta {
    font-size: 10.5px;
    color: var(--mute);
    margin-top: 2px;
  }

  .phone-map {
    margin: 10px 12px;
    height: 200px;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid var(--line);
  }

  .phone-cta {
    position: absolute;
    bottom: 14px;
    left: 14px;
    right: 14px;
    padding: 12px 14px;
    border-radius: 12px;
    background: var(--text);
    color: var(--bg);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .phone-cta-icon {
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--bg);
    font-size: 13px;
    font-weight: 700;
    flex-shrink: 0;
  }

  .phone-cta-title {
    font-size: 12px;
    font-weight: 600;
  }

  .phone-cta-sub {
    font-size: 9.5px;
    color: rgba(11, 13, 16, 0.6);
  }

  .workflow-extras {
    margin-top: 64px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .workflow-extra {
    padding: 24px;
    border-top: 1px solid var(--line-2);
  }

  .workflow-extra-title {
    font-size: 17px;
    font-weight: 600;
  }

  .workflow-extra-desc {
    font-size: 13.5px;
    color: var(--dim);
    margin-top: 8px;
    line-height: 1.55;
  }

  /* ============= FEATURES ============= */
  .feature-grid {
    margin-top: 56px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border: 1px solid var(--line);
    border-radius: 14px;
    overflow: hidden;
  }

  .feature-cell {
    padding: 28px;
    background: var(--panel);
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .feature-cell:nth-child(3n) {
    border-right: none;
  }

  .feature-cell:nth-child(n + 4) {
    border-bottom: none;
  }

  .feature-cell-title {
    font-size: 17px;
    font-weight: 600;
    letter-spacing: -0.012em;
  }

  .feature-cell-desc {
    color: var(--dim);
    font-size: 13.5px;
    line-height: 1.6;
    margin-top: 10px;
  }

  /* ============= FOUNDER ============= */
  .founder {
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .founder-inner {
    max-width: 980px;
    margin: 0 auto;
    padding: 110px 32px;
    text-align: center;
  }

  .founder-quote {
    font-size: clamp(24px, 3.6vw, 36px);
    font-weight: 300;
    line-height: 1.35;
    letter-spacing: -0.014em;
    margin-top: 24px;
    color: var(--text);
    font-style: italic;
  }

  .founder-credit {
    margin-top: 32px;
    display: inline-flex;
    align-items: center;
    gap: 12px;
  }

  .founder-credit-text {
    text-align: left;
  }

  .founder-role {
    color: var(--mute);
    font-size: 12px;
  }

  /* ============= ROADMAP ============= */
  .roadmap-grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 64px;
    align-items: flex-start;
  }

  .roadmap-h {
    font-size: clamp(32px, 4.2vw, 48px);
    font-weight: 400;
    margin: 0;
    letter-spacing: -0.022em;
    line-height: 1.05;
  }

  .roadmap-lede {
    font-size: 16px;
    color: var(--dim);
    margin-top: 20px;
    line-height: 1.6;
  }

  .roadmap-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 32px;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .roadmap-row {
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .roadmap-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .roadmap-dot.done {
    background: var(--accent);
  }

  .roadmap-dot.current {
    background: transparent;
    border: 2px solid var(--accent);
  }

  .roadmap-dot.future {
    background: var(--faint);
  }

  .roadmap-month {
    font-size: 11px;
    color: var(--mute);
    letter-spacing: 0.08em;
    width: 100px;
  }

  .roadmap-label {
    font-size: 15px;
    font-weight: 400;
    color: var(--dim);
  }

  .roadmap-label.done {
    color: var(--text);
  }

  .roadmap-label.current {
    color: var(--accent);
    font-weight: 600;
  }

  .roadmap-now {
    font-size: 10px;
    color: var(--accent);
    margin-left: auto;
    letter-spacing: 0.08em;
  }

  /* ============= PRICING ============= */
  .pricing {
    border-top: 1px solid var(--line);
    padding: 120px 0;
    background: linear-gradient(180deg, #0B0D10 0%, #0E1115 100%);
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 56px;
    max-width: 880px;
  }

  .pricing-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 32px;
    position: relative;
  }

  .pricing-card.highlight {
    background: var(--panel-2);
    border-color: var(--accent);
  }

  .pricing-card-head {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .pricing-card-name {
    font-size: 20px;
    font-weight: 600;
    letter-spacing: -0.012em;
  }

  .pricing-card-rec {
    font-size: 10px;
    color: var(--accent);
    letter-spacing: 0.10em;
  }

  .pricing-card-pitch {
    font-size: 13.5px;
    color: var(--dim);
    margin-top: 8px;
    line-height: 1.5;
  }

  .pricing-card-price {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-top: 24px;
  }

  .pricing-card-amount {
    font-size: 42px;
    font-weight: 500;
    letter-spacing: -0.025em;
    line-height: 1;
  }

  .pricing-card-cadence {
    font-size: 12px;
    color: var(--mute);
  }

  .pricing-card-feats {
    margin-top: 24px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .pricing-card-feat {
    display: flex;
    gap: 10px;
    align-items: flex-start;
    font-size: 13.5px;
    line-height: 1.45;
  }

  .pricing-card-bullet {
    color: var(--accent);
    flex-shrink: 0;
    margin-top: 1px;
  }

  /* ============= FAQ ============= */
  .faq {
    padding: 120px 0;
  }

  .faq-container {
    max-width: 1080px;
  }

  .faq-list {
    margin-top: 56px;
  }

  .faq-item {
    border-top: 1px solid var(--line);
    padding: 22px 0;
  }

  .faq-item:last-child {
    border-bottom: 1px solid var(--line);
  }

  .faq-item summary {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 24px;
    cursor: pointer;
    list-style: none;
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-q {
    font-size: 18px;
    font-weight: 500;
    letter-spacing: -0.012em;
    flex: 1;
  }

  .faq-toggle {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    border: 1px solid var(--line-2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    color: var(--accent);
    flex-shrink: 0;
    line-height: 1;
  }

  .faq-toggle-minus {
    display: none;
  }

  .faq-item[open] .faq-toggle-plus {
    display: none;
  }

  .faq-item[open] .faq-toggle-minus {
    display: inline;
  }

  .faq-a {
    font-size: 15px;
    color: var(--dim);
    margin-top: 14px;
    line-height: 1.65;
    max-width: 760px;
  }

  /* ============= FINAL CTA ============= */
  .final-cta {
    border-top: 1px solid var(--line);
  }

  .final-cta-inner {
    max-width: 1080px;
    margin: 0 auto;
    padding: 130px 32px;
    text-align: center;
  }

  .final-cta-h {
    font-size: clamp(48px, 8vw, 84px);
    font-weight: 400;
    margin: 24px auto 0;
    letter-spacing: -0.028em;
    line-height: 1;
    max-width: 940px;
  }

  .final-cta-h em {
    color: var(--accent);
    font-style: italic;
  }

  .final-cta-lede {
    font-size: 17px;
    color: var(--dim);
    max-width: 540px;
    margin: 24px auto 0;
    line-height: 1.55;
  }

  .final-cta-form {
    margin-top: 36px;
    display: flex;
    justify-content: center;
  }

  /* ============= RESPONSIVE ============= */
  @media (max-width: 1100px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 48px;
      padding: 60px 32px 80px;
    }
    .planner-deep-grid {
      grid-template-columns: 1fr;
    }
    .workflow-sync {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .workflow-arrow {
      flex-direction: row;
    }
    .workflow-arrow svg {
      transform: rotate(90deg);
    }
    .roadmap-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }

  @media (max-width: 880px) {
    .section {
      padding: 80px 0;
    }
    .planner-deep,
    .workflow,
    .pricing,
    .faq {
      padding: 80px 0;
    }
    .pillar-grid,
    .feature-grid,
    .workflow-steps,
    .workflow-extras,
    .pricing-grid {
      grid-template-columns: 1fr;
    }
    .quality-grid {
      grid-template-columns: 1fr 1fr;
    }
    .feature-cell {
      border-right: none;
    }
    .feature-cell:not(:last-child) {
      border-bottom: 1px solid var(--line);
    }
    .feature-cell:last-child {
      border-bottom: none;
    }
    .hero-panel-body {
      grid-template-columns: 1fr;
      min-height: 0;
    }
    .hero-panel-sidebar {
      border-right: none;
      border-bottom: 1px solid var(--line);
    }
    .hero-panel-map {
      min-height: 320px;
    }
    .map-overlay-bottom {
      flex-wrap: wrap;
      gap: 12px;
    }
    .map-overlay-divider {
      display: none;
    }
    .trust-inner {
      gap: 20px;
      padding: 24px;
    }
    .trust-logos {
      gap: 18px;
    }
    .container {
      padding: 0 24px;
    }
  }

  @media (max-width: 560px) {
    .quality-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) {
          e.target.classList.add("visible");
          observer.unobserve(e.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: "0px 0px -50px 0px" }
  );
  document.querySelectorAll(".fade-up").forEach((el) => observer.observe(el));
</script>
